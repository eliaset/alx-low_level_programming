#include "main.h"
/**
 * _islower - check if alphabet is lower case
 * c - place holder
 *Return: 0 1 to exit
 *
 */

int _islower(int c)
{
	int n;

	if (n > 97 && n < 122)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
